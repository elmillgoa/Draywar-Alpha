# Session 028 — 2026-08-07

**REPAIR-5: pause freezes the sim; alt-tab freezes without opening a docked pause menu**

Append-only. Newest entry at the bottom. Do not edit entries above.

---

### `CONTRACT` · 028#1 · 23:01 · REPAIR-5 pause freezes sim + focus-loss without canceling intentional pause

Attempt 2. SceneTree.paused driven from _set_pause and Resume bus path. Main ALWAYS; sim nodes PAUSABLE via _mark_sim_pausable; session overlays ALWAYS; WorldClock PAUSABLE. Focus via _notification four constants. _pause_from_focus_loss so Escape pause survives alt-tab; focus-in refuses under Options/map/sheet/journal. Sector map gated on pause/options (#57). Pause open blocked while docked (#58) — no load-path change, no Job 3 dependency. Tests drive notification() and assert can_process on fuel/hull/world/ship. Red-proofed: remove PAUSABLE demotion fails freeze asserts; delete _notification fails focus tests; always-resume focus-in fails intentional-pause and options-overlay tests.

---

### `CONTRACT` · 028#2 · 23:5x · REPAIR-5 attempt 3 — the docked rule moves to the door

Attempt 2 was rejected: it re-broke #58. Its focus-loss handler called _set_pause(true) with no docked check, so alt-tab at a berth put the pause menu (Load on it) over the frozen station menu — worse than the original, because StationMenu is PAUSABLE and freezes underneath.

Root cause: "no pause while docked" was written into each caller of _set_pause, not into _set_pause. Two callers can open a pause. Attempt 1 guarded both; attempt 2 was a from-scratch rewrite off the same base commit (it never saw attempt 1's code) and guarded one. Nothing failed, because #58 was only ever tested through the Escape key.

Fix: the rule lives in _set_pause via _may_open_pause_menu and nowhere else. _set_pause returns bool so a refusal is distinguishable from a no-op; _unhandled_input no longer keeps its own copy; _on_pause_changed_bus asks the same question. Freezing and menu-opening split into two flags — _focus_freeze plus _pause_open, with _apply_tree_paused freezing for either — so focus loss at a berth freezes the sim (#47) without opening a menu (#58).

Tests: #58 asserted at the door (_set_pause direct) and on all three routes to it. Hostile assertions made unconditional by spawning one via SystemWorld.spawn_hostile_at — the start system has none, so the old `if hostile != null` guards never ran and #46's headline word was unasserted. New test walks Main's real children under a paused tree, because Main being ALWAYS makes the demotion opt-out and a later brief's node would silently keep simulating.

Not fixed, filed as a proposed finding: PauseMenu sets its own visible off on_pause_changed and connects after Main, so an outside emitter of true would raise the panel while docked. No such emitter exists today.

Full suite 888/888, 99 scripts, import + smoke clean.

# EventBus signal catalog

Every signal on `EventBus` is listed here in the same commit that adds it.

| Signal | Payload | Emitters | Listeners |
|--------|---------|----------|-----------|
| *(none yet)* | | | |

Skeleton phase (A0): bus exists; no signals until a contract needs them.

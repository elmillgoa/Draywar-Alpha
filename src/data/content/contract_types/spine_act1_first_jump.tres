[gd_resource type="Resource" script_class="ContractType" load_steps=2 format=3]

; Act I — first jump: Alpha Port → Beta Hub courier. Teach the gate.
[ext_resource type="Script" path="res://src/data/shapes/ContractType.gd" id="1"]

[resource]
script = ExtResource("1")
id = &"spine_act1_first_jump"
display_name = "Sealed pouch — Alpha to Beta Hub"
offering_entity_id = &"entity_reach_authority"
kind = &"delivery"
standing_complete = 8.0
standing_fail = -3.0
standing_abandon = -8.0
pay_credits = 140
destination_station_id = &"station_beta_hub"
is_spine = true
spine_act = 1
requires_flags = PackedStringArray("flag_wake_done")
sets_flags = PackedStringArray("flag_jump_lesson")
min_entity_standing = -100.0
requires_debt = false
offer_station_id = &"station_alpha_port"
journal_blurb = "Reach wants a pouch on Beta Hub before the next shift board. Same dock you woke on; destination is another system. Line up on the gate, jump, fly the approach, dock. The board at Beta will look different. The pouch does not care who owns the station — only that it arrives sealed."
sort_index = 20

[gd_resource type="Resource" script_class="ContractType" load_steps=2 format=3]

; Act II lane — shadow: smuggle munitions Beta → Gamma.
[ext_resource type="Script" path="res://src/data/shapes/ContractType.gd" id="1"]

[resource]
script = ExtResource("1")
id = &"spine_act2_lane_shadow"
display_name = "Lane: Quiet munitions — Hub to Gamma"
offering_entity_id = &"entity_beta_syndicate"
kind = &"smuggle"
standing_complete = 12.0
standing_fail = -5.0
standing_abandon = -12.0
pay_credits = 260
destination_station_id = &"station_gamma_outpost"
cargo_commodity_id = &"commodity_munitions"
cargo_quantity = 4
is_spine = true
spine_act = 2
requires_flags = PackedStringArray("flag_act1_done")
sets_flags = PackedStringArray("flag_lane_shadow")
min_entity_standing = -100.0
requires_debt = false
offer_station_id = &"station_beta_hub"
journal_blurb = "A quiet crate of munitions for Gamma Outpost. Hold stays loaded on accept; Reach docks will seize contraband if you get searched. This lane is margins and risk. Choose it and the clean charter and bounty lanes go dark. Jump smart, dock gray, turn in with the cargo still aboard."
sort_index = 30
